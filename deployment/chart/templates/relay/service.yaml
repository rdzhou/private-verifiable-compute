apiVersion: v1
kind: Service
metadata:
  name: {{ include "pvc.relay.name" . }}
  labels:
    {{- include "pvc.relay.labels" . | nindent 4 }}
  namespace: {{ .Values.global.namespace }}
spec:
  type: {{ if eq .Values.platform "kata" }}NodePort{{ else }}{{ .Values.relay.service.type }}{{ end }}
  ports:
    - port: {{ .Values.relay.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pvc.relay.selectorLabels" . | nindent 4 }}
