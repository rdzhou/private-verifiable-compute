apiVersion: v1
kind: Service
metadata:
  name: {{ include "pvc.identity-server.name" . }}
  labels:
    {{- include "pvc.identity-server.labels" . | nindent 4 }}
  namespace: {{ .Values.global.namespace }}
spec:
  type: {{ if eq .Values.platform "kata" }}NodePort{{ else }}{{ .Values.identityServer.service.type }}{{ end }}
  ports:
    - port: {{ .Values.identityServer.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pvc.identity-server.selectorLabels" . | nindent 4 }}
